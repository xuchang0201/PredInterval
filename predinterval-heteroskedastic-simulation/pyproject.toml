[project]
name = "predinterval-normalization"
version = "0.1.0"
description = "Standalone reproduction of the normalization and PredInterval analyses"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.4",
    "pandas>=3.0.2",
    "scikit-learn>=1.8.0",
    "scipy>=1.17.1",
    "tqdm>=4.67.3",
]

[tool.uv]
package = false
